{
  "name": "fxhash-subgraph",
  "version": "0.0.0",
  "license": "GPL-3.0",
  "main": "build/index.js",
  "directories": {
    "src": "src"
  },
  "scripts": {
    "graph-node": "docker-compose up",
    "clean": "rimraf ./src/types ./build subgraph.yaml",
    "prepare:local": "mustache config/local.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:hardhat": "mustache config/hardhat.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:rinkeby": "mustache config/rinkeby.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:mainnet": "mustache config/mainnet.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:goerli": "mustache config/goerli.json subgraph.yaml.mustache > subgraph.yaml",
    "codegen": "graph codegen --output-dir src/types/",
    "test:configgen": "echo libsFolder: $(dirname `cd ../..; pwd`)/fxhash-subgraph/node_modules > matchstick.yaml",
    "test": "graph test",
    "create:localnode": "graph create --node http://localhost:8020/",
    "create-local": "graph create --node http://localhost:8020/ fxhash/fxhash-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ fxhash/fxhash-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fxhash/fxhash-subgraph",
    "deploy": "graph deploy --debug --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "deploy:localnode": "yarn clean && yarn prepare:local && yarn codegen && graph deploy fxhash/fxhash-subgraph --debug --node http://localhost:8020/ --ipfs http://localhost:5001/",
    "deploy:hardhat": "yarn clean && yarn prepare:hardhat && yarn codegen && yarn create:localnode fxhash/fxhash-subgraph && yarn deploy:localnode fxhash/fxhash-subgraph",
    "deploy:rinkeby": "yarn clean && yarn prepare:rinkeby && yarn codegen && yarn deploy fxhash/fxhash-subgraph",
    "deploy:mainnet": "yarn clean && yarn prepare:mainnet && yarn codegen && yarn deploy fxhash/fxhash-subgraph",
    "deploy:goerli": "yarn clean && yarn prepare:goerli && yarn codegen && yarn deploy fxhash/fxhash-subgraph",
    "mustache": "mustache"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.52.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "matchstick-as": "^0.5.2",
    "mustache": "^4.2.0"
  }
}
